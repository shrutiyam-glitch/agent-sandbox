apiVersion: extensions.agents.x-k8s.io/v1alpha1
kind: SandboxClaim
metadata:
  name: sandbox-claim-new-3
  namespace: sandbox-test
  labels:
    app: agent-sandbox-workload
spec:
  sandboxTemplateRef:
    name: python-counter-template
  additionalPodMetadata: 
    annotations:
      hello: "world"
    labels:
      tenant: test-tenant
      user: test-user